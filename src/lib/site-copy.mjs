/**
 * 每个 topic 必须重写的正文文案。
 *
 * 只收「短、纯文本、换 topic 必改」的串。含链接或结构的段落留在页面原处,
 * 由源码里的 `TOPIC:` 注释标记——把带 <a> 的整段搬进本文件只会得到
 * 一堆难读的 HTML 字符串,不如让作者直接在页面里改。
 *
 * 完整的换 topic 步骤见 docs/runbooks/new-topic.md。
 */
export const COPY = {
	home: {
		/** hero 上方的小标签,一句话概括路线跨度。 */
		kicker: "从组合到单子",
		/** hero 主标题,逐行给出;渲染时行间插 <br />。 */
		titleLines: ["程序为什么", "能组合"],
		/** hero 导语,一到两句说明本库覆盖什么、代码用什么语言。 */
		lead: "一个写给未来的自己的结构化学习库。从「组合是编程的本质」出发，逐步抽象到函子、自然变换、极限、伴随与单子；每个概念先在 Rust 里落地一次。",
	},

	about: {
		/** 关于页标题下方的一句话定位。 */
		lead: "一个持续编写并核验的中文范畴论学习库，蒸馏自《Category Theory for Programmers》。",
		/** 「这是什么」一节的首段:学习路线的完整跨度与代码语言约定。 */
		scope:
			"本站写给未来的自己。路线从集合、函数与类型记号开始，经过范畴、函子、自然变换、极限与 Yoneda 引理，最终抵达伴随、单子与 F-代数。示例代码以 Rust 为主，Haskell 只在表达力对照处出场。",
	},

	learn: {
		kicker: "第一遍主线 · 无状态入口",
		/** 学习路径页主标题,点明「先看什么、后补什么」。 */
		title: "先看到组合，再回补需要的记号。",
		/** 导语。coreCount 是记号前置层的词条数,由页面传入。 */
		leadFor: (coreCount) =>
			`这条路径按 CTFP 的教学顺序走完范畴论主干。记号前置只有 ${coreCount} 篇速查，读到能认出记号即可；你可以沿主线继续，不需要先精通 Haskell 或类型理论。`,
		/** 主标题下的题记,复述一遍阅读顺序的理由。 */
		epigraph: "先让「组合」的直觉出现，再为当前概念补上必要的形式化。",
		/** 「三种动作」列表里第二条的说明,指向本 topic 的前置层叫什么。 */
		backfillHint: "只打开当前词条真正引用的记号。",
		/** 页尾主按钮:指向前置层所在章节。 */
		footerCta: { sectionDir: "notation", label: "查看记号地基" },
		/** 前置层在路径里的兜底名称(learning-paths.yaml 未给 name_zh 时使用)。 */
		coreLayerFallback: "最小记号前置",
	},
};
